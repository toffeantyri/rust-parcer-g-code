//! Доменные трейты: контракты для внешних зависимостей

use crate::domain::Token;

/// Контракт лексера — преобразует сырой текст G-кода в последовательность токенов.
pub trait Lexer {
    /// Разбирает входную строку G-кода в вектор токенов.
    fn tokenize(&self, input: &str) -> Vec<Token>;
}
