[package]
name = "code_parser"
version = "0.1.0"
edition = "2021"

description = "A parser and formatter library in Rust"

[lib]
name = "code_parser"

[[bin]]
name = "parser_cli"
path = "src/bin/main.rs"

[dependencies]
